"use client"; import { useState, useRef, useEffect, KeyboardEvent } from "react"; import { Bot, X, MoveRight, Loader2 } from "lucide-react"; import { cn } from "@/lib/utils"; import gsap from "gsap"; import { useGSAP } from "@gsap/react"; import { useRouter } from "next/navigation"; import { aiApi } from "@/features/ai-stylist/api/ai.api"; import { ChatMessage, ChatSessionRes } from "@/features/ai-stylist/types"; import { useChatSessions } from "@/features/ai-stylist/queries/ai.queries"; import { toast } from "sonner"; import { AIChatMessage } from "@/features/ai-stylist/components/AIChatMessage"; import { AIChatHistorySidebar } from "@/features/ai-stylist/components/AIChatHistorySidebar"; gsap.registerPlugin(useGSAP); export function GlobalAIChat() { const [isOpen, setIsOpen] = useState(false); const containerRef = useRef<HTMLDivElement>(null); const chatContainerRef = useRef<HTMLDivElement>(null); const chatEndRef = useRef<HTMLDivElement>(null); const router = useRouter(); const [isHistoryOpen, setIsHistoryOpen] = useState(false); const { data: chatSessionsData, isLoading: isLoadingSessions } = useChatSessions(); const [isChatting, setIsChatting] = useState(false); const [chatMessages, setChatMessages] = useState<ChatMessage[]>([]); const [contextID, setContextID] = useState<string>(""); const [chatInput, setChatInput] = useState(""); const abortControllerRef = useRef<AbortController | null>(null); useEffect(() => { if (chatMessages.length > 0 || isChatting) { chatEndRef.current?.scrollIntoView({ behavior: 'smooth', block: 'nearest' }); } }, [chatMessages, isChatting]); useEffect(() => { return () => { if (abortControllerRef.current) { abortControllerRef.current.abort(); } }; }, []); const { contextSafe } = useGSAP({ scope: containerRef }); useEffect(() => { if (!chatContainerRef.current) return; if (isOpen) { gsap.fromTo(chatContainerRef.current, { y: 50, opacity: 0, scale: 0.95, transformOrigin: "bottom right", display: "flex" }, { y: 0, opacity: 1, scale: 1, duration: 0.4, ease: "back.out(1.2)" } ); } else { gsap.to(chatContainerRef.current, { y: 50, opacity: 0, scale: 0.95, duration: 0.3, ease: "power2.in", onComplete: () => { gsap.set(chatContainerRef.current, { display: "none" }); } }); } }, [isOpen]); const handleSelectSession = async (session: ChatSessionRes) => { setIsHistoryOpen(false); setContextID(session.id); setChatMessages([]); try { toast.loading("Đang tải lịch sử...", { id: "load_history" }); const res = await aiApi.getChatMessages(session.id, { limit: 100 }); const formattedMessages: ChatMessage[] = res.items.map((msg, index) => ({ id: msg.id, role: (msg.sender.toLowerCase() === 'user' ? 'user' : 'ai') as 'user' | 'ai', content: msg.content, timestamp: new Date(msg.createdAt).getTime(), _rawTime: msg.createdAt, _originalIndex: index })).sort((a, b) => { const timeDiff = a._rawTime.localeCompare(b._rawTime); if (timeDiff !== 0) return timeDiff; return b._originalIndex - a._originalIndex; }); setChatMessages(formattedMessages); toast.success("Đã tải lịch sử trò chuyện", { id: "load_history" }); } catch (error: any) { console.error(error); toast.error("Không thể tải lịch sử", { id: "load_history" }); } }; const handleNewChat = () => { setContextID(""); setChatMessages([]); setChatInput(""); setIsHistoryOpen(false); }; const handleSendMessage = async () => { if (isChatting) return; let finalInput = chatInput.trim(); if (!finalInput) return; const normalizedInput = finalInput.normalize('NFC'); if (normalizedInput.length > 2000) { toast.error(`Tin nhắn quá dài (${normalizedInput.length}/2000 ký tự). Vui lòng rút gọn nội dung.`); return; } const userMsg: ChatMessage = { id: crypto.randomUUID(), role: 'user', content: finalInput, timestamp: Date.now() }; setChatMessages(prev => [...prev, userMsg]); setChatInput(""); setIsChatting(true); let currentContextID = contextID; if (!currentContextID) { try { const session = await aiApi.createChatSession({ title: finalInput.slice(0, 30) }); setContextID(session.id); currentContextID = session.id; } catch (e) { console.error("Failed to create chat session", e); setIsChatting(false); return; } } const aiMsgId = crypto.randomUUID(); setChatMessages(prev => [ ...prev, { id: aiMsgId, role: 'ai', content: '', timestamp: Date.now() } ]); abortControllerRef.current = new AbortController(); await aiApi.sendChatMessageStream( currentContextID, userMsg.content, (chunk) => { setChatMessages(prev => prev.map(msg => msg.id === aiMsgId ? { ...msg, content: msg.content + chunk } : msg) ); }, (fullText) => { setIsChatting(false); setChatMessages(prev => prev.map(msg => msg.id === aiMsgId ? { ...msg, content: fullText } : msg) ); }, (error) => { console.error("Lỗi stream chat:", error); setIsChatting(false); }, abortControllerRef.current.signal ); }; const handleKeyDown = (e: KeyboardEvent<HTMLInputElement>) => { if (e.key === 'Enter') { e.preventDefault(); handleSendMessage(); } }; const handleRedirectToGenerate = (details?: string) => { setIsOpen(false); if (details) { // const searchParams = new URLSearchParams(); // searchParams.set('details', details); // router.push(`/ai-stylist?${searchParams.toString()}`); router.push(`/ai-stylist`); } else { router.push('/ai-stylist'); } }; return ( <div ref={containerRef} className="fixed bottom-24 md:bottom-6 right-4 md:right-6 z-[100] flex flex-col items-end justify-end pointer-events-none gap-4"> {/* Chat Window */} <div ref={chatContainerRef} className={cn( "w-[350px] md:w-[400px] h-[600px] max-h-[80vh] bg-white border border-[#E5E5E5] shadow-2xl flex flex-col overflow-hidden pointer-events-auto relative origin-bottom-right mb-4 rounded-xl", !isOpen && "hidden" )} > <AIChatHistorySidebar isHistoryOpen={isHistoryOpen} setIsHistoryOpen={setIsHistoryOpen} isLoadingSessions={isLoadingSessions} chatSessionsData={chatSessionsData} handleNewChat={handleNewChat} handleSelectSession={handleSelectSession} contextID={contextID} /> {/* Header */} <div className="flex items-center justify-between p-4 border-b border-[#E5E5E5] bg-[#F9F9F9]"> <div className="flex items-center gap-2"> <Bot className="w-5 h-5 text-[#1A1A1A]" /> <h3 className="font-bold text-[13px] text-[#1A1A1A] ">CLOSY AI STYLIST</h3> </div> <button onClick={() => setIsOpen(false)} className="text-[#A3A3A3] hover:text-[#1A1A1A] transition-colors p-1"> <X className="w-4 h-4" /> </button> </div> {/* Messages */} <div className="flex-1 p-4 overflow-y-auto space-y-4 bg-white"> {chatMessages.length === 0 ? ( <div className="flex flex-col gap-2 items-start mt-4"> <span className="text-[10px] font-bold text-[#A3A3A3]">CLOSY AI</span> <div className="bg-[#F9F9F9] text-[#1A1A1A] border border-[#E5E5E5] p-3 rounded-lg text-[13px] leading-relaxed"> Xin chào! Tôi có thể giúp gì cho bạn hôm nay? </div> </div> ) : ( chatMessages.map((msg) => ( <AIChatMessage key={msg.id} msg={msg} isChatting={isChatting} isGenerating={false} onGenerateRedirect={handleRedirectToGenerate} fullChatContext={chatMessages.filter(m => m.role === 'user').map(m => m.content).join('. ')} /> )) )} <div ref={chatEndRef} /> </div> {/* Input Area */} <div className="p-3 border-t border-[#E5E5E5] bg-white relative flex flex-col"> <div className="relative border border-[#E5E5E5] rounded-xl overflow-hidden focus-within:border-[#1A1A1A] transition-colors bg-[#F9F9F9]"> <input value={chatInput} onChange={(e) => setChatInput(e.target.value)} onKeyDown={handleKeyDown} disabled={isChatting} className="w-full bg-transparent border-none outline-none px-3 py-3 text-[13px] text-[#1A1A1A] placeholder:text-[#A3A3A3] disabled:opacity-50" placeholder="Hỏi AI tư vấn phối đồ..." type="text" maxLength={2000} /> <button onClick={handleSendMessage} disabled={isChatting || !chatInput.trim()} className="absolute right-1 top-1/2 -translate-y-1/2 w-8 h-8 flex items-center justify-center bg-[#1A1A1A] text-white rounded-lg hover:bg-black transition-colors disabled:opacity-50" > <MoveRight className="w-4 h-4" /> </button> </div> <button onClick={() => setIsHistoryOpen(!isHistoryOpen)} className="text-[10px] font-bold text-[#A3A3A3] hover:text-[#1A1A1A] mt-2 self-start px-1" > {isHistoryOpen ? "Đóng Lịch sử" : "Xem Lịch sử Chat"} </button> </div> </div> {/* FAB */} <button onClick={() => setIsOpen(!isOpen)} className="w-14 h-14 bg-[#1A1A1A] hover:bg-black text-white rounded-full flex items-center justify-center shadow-xl hover:shadow-2xl hover:scale-105 transition-all pointer-events-auto shrink-0" > {isOpen ? <X className="w-6 h-6" /> : <Bot className="w-6 h-6" />} </button> </div> ); } 