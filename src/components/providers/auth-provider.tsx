"use client"; import { useEffect } from "react"; import { useProfile } from "@/features/profile/queries/profile.queries"; import { useAuthStore } from "@/store/useAuthStore"; export function AuthProvider({ children }: { children: React.ReactNode }) { const { data: profileData, isError, isLoading } = useProfile(); const setUser = useAuthStore((state) => state.setUser); useEffect(() => { if (profileData) { // Sync fetched profile data to global store // Populate mock backward compatible fields const userToStore = { ...profileData, name: profileData.firstName + (profileData.lastName ? ` ${profileData.lastName}` : ""), avatar: `https://api.dicebear.com/7.x/notionists/svg?seed=${profileData.username}`, isPremium: (!!profileData.subscription?.planSlug && profileData.subscription.planSlug !== "free") || (!!profileData.planSlug && profileData.planSlug !== "free") }; setUser(userToStore); } }, [profileData, setUser]); useEffect(() => { if (isError) { setUser(null); } }, [isError, setUser]); // Optionally, you can return null while loading if you want to block rendering // but it's usually better to render children and let them handle loading states // or just show a global loader if needed. return <>{children}</>; } 