"use client";
import { useEffect, useState } from "react";
import { useGeolocation } from "@/hooks/useGeolocation";
import { fetchWeather, WeatherData } from "@/services/weather.service";
import { Loader2 } from "lucide-react";
import Image from "next/image";
import { cn } from "@/lib/utils"; // Assuming cn exists, else fallback to template literal interface WeatherWidgetProps { className?: string; } export function WeatherWidget({ className }: WeatherWidgetProps) { const { location } = useGeolocation(); const [weather, setWeather] = useState<WeatherData | null>(null); const [loading, setLoading] = useState(true); useEffect(() => { if (location.latitude && location.longitude) { setLoading(true); fetchWeather(location.latitude, location.longitude) .then(data => setWeather(data)) .catch(err => console.error(err)) .finally(() => setLoading(false)); } else if (!location.loading) { // Failed to get location or fallback IP failed setLoading(false); } }, [location.latitude, location.longitude, location.loading]); if (loading || location.loading) { const defaultClasses = "bg-white/95 rounded-2xl px-4 md:px-5 py-2 md:py-3 shadow-lg border border-white/60 flex items-center gap-2 md:gap-3"; return ( <div className={`${defaultClasses} ${className || ""}`}> <Loader2 className="w-5 h-5 md:w-6 md:h-6 animate-spin text-[#D9C5B2]" /> <div> <div className="text-[10px] md:text-xs font-bold text-[#1A1A1A]">Hôm nay</div> <div className="text-[9px] md:text-[10px] text-[#5A5A5A]">Đang tải...</div> </div> </div> ); } if (!weather) { // UI mặc định nếu lỗi (giống trong mockup) const defaultClasses = "bg-white/95 rounded-2xl px-4 md:px-5 py-2 md:py-3 shadow-lg border border-white/60 flex items-center gap-2 md:gap-3"; return ( <div className={`${defaultClasses} ${className || ""}`}> <span className="text-xl md:text-2xl">☀️</span> <div> <div className="text-[10px] md:text-xs font-bold text-[#1A1A1A]">Hôm nay</div> <div className="text-[9px] md:text-[10px] text-[#5A5A5A]">32°C — Nắng nhẹ</div> </div> </div> ); } const getWeatherStyle = (icon: string) => { const isDay = icon.includes('d'); const code = icon.slice(0, 2); if (!isDay) { // NIGHT MODE if (code === '01') return { bg: 'bg-[#1e1b4b]', border: 'border-indigo-900/50', textTitle: 'text-indigo-50', textSub: 'text-indigo-200' }; if (['02', '03', '04'].includes(code)) return { bg: 'bg-[#1e293b]', border: 'border-slate-700/50', textTitle: 'text-slate-50', textSub: 'text-slate-300' }; if (['09', '10', '11'].includes(code)) return { bg: 'bg-[#172554]', border: 'border-blue-900/50', textTitle: 'text-blue-50', textSub: 'text-blue-200' }; return { bg: 'bg-[#111827]', border: 'border-gray-800/50', textTitle: 'text-gray-50', textSub: 'text-gray-300' }; } else { // DAY MODE if (code === '01') return { bg: 'bg-[#fffbeb]', border: 'border-amber-200', textTitle: 'text-amber-900', textSub: 'text-amber-700/80' }; if (['02', '03', '04'].includes(code)) return { bg: 'bg-[#f8fafc]', border: 'border-slate-200', textTitle: 'text-slate-800', textSub: 'text-slate-500' }; if (['09', '10', '11'].includes(code)) return { bg: 'bg-[#eff6ff]', border: 'border-blue-200', textTitle: 'text-blue-900', textSub: 'text-blue-700/80' }; return { bg: 'bg-white/95', border: 'border-white/60', textTitle: 'text-[#1A1A1A]', textSub: 'text-[#5A5A5A]' }; } }; const getCustomIcon = (iconCode: string) => { const code = iconCode.slice(0, 2); switch (code) { case '01': return '/Weather/01d.png'; // Trời quang case '02': return '/Weather/02d.png'; // Ít mây case '03': return '/Weather/03d.png'; // Mây rải rác case '04': return '/Weather/04d.png'; // Nhiều mây case '09': return '/Weather/05d.png'; // Mưa rào case '10': return '/Weather/06d.png'; // Mưa case '11': return '/Weather/07d.png'; // Dông case '13': return '/Weather/08d.png'; // Tuyết case '50': return '/Weather/09d.png'; // Sương mù default: return '/Weather/01d.png'; } }; const formattedDescription = weather.description.charAt(0).toUpperCase() + weather.description.slice(1); const colors = getWeatherStyle(weather.icon); const defaultClasses = `${colors.bg} rounded-2xl px-4 md:px-5 py-2 md:py-3 shadow-lg border ${colors.border} flex items-center gap-2 md:gap-3 pointer-events-auto cursor-pointer hover:scale-105 transition-all duration-300`; return ( <div className={`${defaultClasses} ${className || ""}`}> <div className="w-8 h-8 md:w-10 md:h-10 relative flex-shrink-0"> <Image src={getCustomIcon(weather.icon)} alt={weather.description} fill className="object-contain drop-shadow-md" unoptimized /> </div> <div> <div className={`text-[10px] md:text-xs font-bold ${colors.textTitle} transition-colors`}>Hôm nay</div> <div className={`text-[9px] md:text-[10px] ${colors.textSub} whitespace-nowrap transition-colors`}>{weather.temp}°C — {formattedDescription}</div> </div> </div> ); }
