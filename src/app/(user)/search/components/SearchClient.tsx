"use client";
import { useState, useEffect } from "react";
import { useRouter, useSearchParams } from "next/navigation";
import { Search, Eye, Heart, MessageCircle, UserPlus, Sparkles, Grid, List } from "lucide-react";
import { Button } from "@/components/ui/button";
import { cn } from "@/lib/utils";
import { useMyWardrobe } from "@/features/wardrobe/queries/wardrobe.queries";
import { getWardrobeItemName } from "@/features/wardrobe/utils";
import Image from "next/image"; // No mock data export function SearchClient() { const router = useRouter(); const searchParams = useSearchParams(); const setSearchParams = (p: any) => { const q = new URLSearchParams(searchParams.toString()); for(const k in p) q.set(k, p[k]); router.push('?' + q.toString()); }; const query = searchParams.get("q") || ""; const activeTab = searchParams.get("type") || "all"; const [searchInput, setSearchInput] = useState(query); useEffect(() => { setSearchInput(query); }, [query]); const handleSearchSubmit = (e: React.FormEvent) => { e.preventDefault(); setSearchParams({ q: searchInput, type: activeTab }); }; const handleTabChange = (tab: string) => { setSearchParams({ q: query, type: tab }); }; // Searching logic const { data: myWardrobeData, isLoading: isSearchLoading } = useMyWardrobe(); const rawItems = myWardrobeData ? myWardrobeData.items : []; const realItems = rawItems.filter((item: any) => { if (!query) return true; const searchTokens = query.toLowerCase().split(/\s+/).filter(Boolean); const itemName = getWardrobeItemName(item).toLowerCase(); const itemText = [ itemName, (item.category?.name || "").toLowerCase(), ((item as any).name || "").toLowerCase(), (item.color || "").toLowerCase(), (item.material || "").toLowerCase(), (item.style || "").toLowerCase(), (item.brand || "").toLowerCase() ].join(" "); // AND logic: Every word typed in the search must exist somewhere in the item properties return searchTokens.every(token => itemText.includes(token)); }); const filteredItems = query ? realItems.map((item: any) => ({ id: item.id, name: item.name || getWardrobeItemName(item), brand: item.brand || "Smart Wardrobe", category: item.category?.name || "", tags: [item.style, item.material, item.seasonality].filter(Boolean), img: item.imageUrl, timesWorn: 0 })) : []; const totalResults = filteredItems.length; return ( <div className="max-w-5xl mx-auto space-y-8 animate-in fade-in duration-500 pb-16 font-sans"> {/* Search Input Box */} <div className="space-y-4"> <h1 className="text-3xl font-sans font-medium tracking-wide text-ink">Tìm kiếm toàn cục</h1> <form onSubmit={handleSearchSubmit} className="relative w-full"> <Search className="absolute left-4 top-1/2 -translate-y-1/2 size-5 text-ink-muted" /> <input type="text" value={searchInput} onChange={(e) => setSearchInput(e.target.value)} placeholder="Tìm kiếm quần áo, bài viết, hoặc người dùng..." className="w-full h-14 pl-12 pr-28 rounded-2xl bg-cream-dark/30 border-transparent focus:ring-1 focus:ring-terracotta focus:border-terracotta text-sm transition-all outline-none" /> <Button type="submit" className="absolute right-2 top-2 h-10 rounded-xl bg-ink text-cream hover:bg-ink/90 px-6 font-sans text-xs " > TÌM KIẾM </Button> </form> </div> {/* Tabs list */} {query && ( <div className="space-y-6"> <div className="flex items-center justify-between border-b border-cream-dark pb-2"> <div className="flex overflow-x-auto gap-4 no-scrollbar"> {[ { label: `Tất cả (${totalResults})`, value: "all" }, { label: `Tủ đồ (${filteredItems.length})`, value: "item" } ].map(tab => ( <button key={tab.value} onClick={() => handleTabChange(tab.value)} className={cn( "pb-3 text-sm font-medium relative transition-all whitespace-nowrap", activeTab === tab.value ? "text-ink font-semibold" : "text-ink-muted hover:text-ink" )} > {tab.label} {activeTab === tab.value && ( <div className="absolute bottom-0 left-0 right-0 h-0.5 bg-terracotta rounded-full" /> )} </button> ))} </div> <p className="text-xs font-sans text-ink-muted"> Tìm thấy {totalResults} kết quả cho "{query}" </p> </div> {/* Results Area */} {totalResults === 0 ? ( <div className="text-center py-16 space-y-3 bg-cream-dark/10 rounded-2xl border border-dashed border-cream-dark max-w-md mx-auto"> <div className="size-12 rounded-full bg-cream-dark flex items-center justify-center mx-auto text-ink-muted"> <Search className="size-6" /> </div> <div className="space-y-1"> <h3 className="font-sans text-lg font-bold text-ink">Không có kết quả</h3> <p className="text-sm text-ink-muted"> Không tìm thấy kết quả nào khớp với "{query}". Vui lòng thử từ khóa khác. </p> </div> </div> ) : ( <div className="space-y-8"> {/* 1. WARDROBE ITEMS SECTION */} {(activeTab === "all" || activeTab === "item") && filteredItems.length > 0 && ( <div className="space-y-4"> <h2 className="text-xs font-sans text-ink font-bold flex items-center gap-2"> <Grid className="size-4 text-terracotta" /> Tủ đồ cá nhân ({filteredItems.length}) </h2> <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4"> {filteredItems.map((item: any) => ( <div key={item.id} onClick={() => router.push(`/wardrobe/item/${item.id}`)} className="group bg-cream-dark/20 hover:bg-cream-dark/30 rounded-xl overflow-hidden border border-cream-dark/50 hover:border-ink-subtle transition-all duration-300 cursor-pointer" > <div className="aspect-[4/5] bg-cream-dark/40 overflow-hidden relative"> <Image fill sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw" src={item.img} alt={item.name} className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" /> <div className="absolute inset-0 bg-ink/20 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center"> <span className="bg-cream text-ink text-xs font-sans px-3 py-1.5 rounded-lg flex items-center gap-1 shadow-sm font-semibold"> <Eye className="size-3.5" /> CHI TIẾT </span> </div> </div> <div className="p-3"> <h4 className="font-medium text-sm text-ink truncate">{item.name}</h4> <div className="flex items-center justify-between text-[10px] text-ink-muted mt-1 font-sans"> <span>{item.brand}</span> <span>Mặc {item.timesWorn} lần</span> </div> </div> </div> ))} </div> </div> )} </div> )} </div> )} {/* Empty State before search */} {!query && ( <div className="text-center py-20 max-w-md mx-auto space-y-4"> <div className="size-16 rounded-full bg-cream-dark/30 flex items-center justify-center mx-auto text-ink-muted"> <Search className="size-8" /> </div> <div className="space-y-1"> <h2 className="font-sans text-xl font-bold text-ink">Bắt đầu tìm kiếm</h2> <p className="text-sm text-ink-muted"> Nhập từ khóa ở ô phía trên để tra cứu quần áo trong tủ cá nhân, bài viết từ cộng đồng, hay hồ sơ người dùng khác. </p> </div> </div> )} </div> ); }
