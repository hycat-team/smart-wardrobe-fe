import { Metadata } from "next"; import { serverFetch } from "@/lib/server-fetch"; import { WardrobeItemRes as WardrobeItem } from "@/features/wardrobe/types"; import { WardrobeItemChỉnh sửaClient } from "../components/WardrobeItemChỉnh sửaClient"; export const metadata: Metadata = { title: "Chỉnh sửa trang phục | Smart Wardrobe", description: "Chỉnh sửa thông tin chi tiết trang phục của bạn.", }; export default async function WardrobeItemChỉnh sửaPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const item = await serverFetch<WardrobeItem>(`/wardrobe-items/${id}`); return <WardrobeItemChỉnh sửaClient itemId={id} initialItem={item || undefined} />; } 