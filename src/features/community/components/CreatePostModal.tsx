'use client'; import React, { useState, useRef } from 'react'; import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from '@/components/ui/dialog'; import { Button } from '@/components/ui/button'; import { Plus, Image as ImageIcon, X, Loader2 } from 'lucide-react'; import { useCreatePost } from '../queries/community.queries'; import { communityApi } from '../api/community.api'; import { toast } from 'sonner'; import { uploadToCloudinary } from '@/lib/cloudinary'; import Image from 'next/image'; export const CreatePostModal = () => { const [open, setOpen] = useState(false); const [title, setTitle] = useState(''); const [content, setContent] = useState(''); const [images, setImages] = useState<File[]>([]); const [isUploading, setIsUploading] = useState(false); const fileInputRef = useRef<HTMLInputElement>(null); const { mutate: createPost, isPending } = useCreatePost(); const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => { if (e.target.files && e.target.files.length > 0) { const newFiles = Array.from(e.target.files); setImages((prev) => [...prev, ...newFiles]); } }; const handleRemoveImage = (index: number) => { setImages((prev) => prev.filter((_, i) => i !== index)); }; const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); if (!title.trim() || !content.trim()) return; try { setIsUploading(true); const mediaList = []; // Upload images to Cloudinary for (let i = 0; i < images.length; i++) { const file = images[i]; const signatureRes = await communityApi.getPostUploadSignature(); const cloudinaryData = await uploadToCloudinary({ file, signatureParams: { apiKey: signatureRes.apiKey, timestamp: signatureRes.timestamp, signature: signatureRes.signature, folder: signatureRes.folder, publicId: signatureRes.publicId, }, }); mediaList.push({ mediaType: "IMAGE", mediaUrl: cloudinaryData.secure_url, publicId: cloudinaryData.public_id, sortOrder: i, }); } // Submit the post createPost( { title, content, postType: 'OUTFIT', media: mediaList, items: [], }, { onSuccess: () => { setOpen(false); setTitle(''); setContent(''); setImages([]); }, onSettled: () => { setIsUploading(false); } } ); } catch (error) { toast.error('Có lỗi xảy ra khi tải ảnh lên.'); setIsUploading(false); } }; return ( <Dialog open={open} onOpenChange={setOpen}> <DialogTrigger asChild> <Button className="rounded-full bg-primary text-primary-foreground hover:bg-primary/90 hover:-translate-y-0.5 active:translate-y-0 transition-all duration-300 shadow-xl shadow-primary/10 gap-2 h-11 px-6 font-medium tracking-wide text-[13px]" size="default"> <Plus className="size-4" /> <span>Tạo bài viết</span> </Button> </DialogTrigger> <DialogContent className="sm:max-w-[525px] p-0 overflow-hidden bg-surface"> <DialogHeader className="px-6 py-4 border-b border-border/40"> <DialogTitle className="text-xl font-semibold text-primary">Tạo bài viết mới</DialogTitle> </DialogHeader> <form onSubmit={handleSubmit} className="flex flex-col"> <div className="px-6 py-4 space-y-4 max-h-[60vh] overflow-y-auto no-scrollbar"> <div> <input type="text" placeholder="Tiêu đề bài viết..." value={title} onChange={(e) => setTitle(e.target.value)} className="w-full text-lg font-medium bg-transparent border-0 border-b border-transparent focus:border-border/50 focus:ring-0 placeholder:text-muted-foreground outline-none px-0 py-2 transition-colors" autoFocus /> </div> <div> <textarea placeholder="Chia sẻ phong cách, ý tưởng hoặc trải nghiệm của bạn..." value={content} onChange={(e) => setContent(e.target.value)} rows={5} className="w-full text-sm bg-transparent border-0 focus:ring-0 placeholder:text-muted-foreground outline-none resize-none px-0 py-2" /> </div> {/* Image Preview Grid */} {images.length > 0 && ( <div className="grid grid-cols-3 gap-2 mt-4"> {images.map((file, index) => ( <div key={index} className="relative aspect-square rounded-md overflow-hidden bg-muted"> <Image fill sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw" src={URL.createObjectURL(file)} alt="preview" className="w-full h-full object-cover" /> <button type="button" onClick={() => handleRemoveImage(index)} className="absolute top-1 right-1 bg-black/50 hover:bg-black/70 text-white rounded-full p-1 transition-colors" > <X className="w-3 h-3" /> </button> </div> ))} </div> )} <div className="flex items-center pt-2"> <input type="file" ref={fileInputRef} onChange={handleFileChange} accept="image/*" multiple className="hidden" /> <Button type="button" variant="ghost" size="sm" className="text-muted-foreground hover:text-primary gap-2 px-2" onClick={() => fileInputRef.current?.click()} > <ImageIcon className="size-4" /> <span>Thêm hình ảnh</span> </Button> </div> </div> <div className="px-6 py-4 bg-muted/20 border-t border-border/40 flex justify-end gap-3"> <Button type="button" variant="ghost" onClick={() => setOpen(false)}> Hủy </Button> <Button type="submit" disabled={isPending || isUploading || !title.trim() || !content.trim()}> {isPending || isUploading ? ( <> <Loader2 className="mr-2 h-4 w-4 animate-spin" /> {isUploading ? 'Đang tải ảnh...' : 'Đang đăng...'} </> ) : ( 'Đăng bài' )} </Button> </div> </form> </DialogContent> </Dialog> ); }; 