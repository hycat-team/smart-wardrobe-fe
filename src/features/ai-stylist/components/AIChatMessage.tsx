import { useRef } from "react";
import { cn } from "@/lib/utils";
import gsap from "gsap";
import { useGSAP } from "@gsap/react";
import { Sparkles } from "lucide-react";
import { ChatMessage } from "@/features/ai-stylist/types";
gsap.registerPlugin(useGSAP); // Hàm tiện ích để format tin nhắn từ AI: Xử lý xuống dòng, bỏ kí tự đặc biệt thừa const formatAIResponse = (text: string) => { if (!text) return null; // Bỏ các dấu ngoặc kép thừa ở đầu/cuối hoặc kí tự escape \" let cleaned = text.replace(/^"+|"+$/g, '').replace(/\\"/g, '"'); // Format markdown: in đậm + nghiêng (***text***), in đậm (**text**), in nghiêng (*text*) cleaned = cleaned .replace(/\*\*\*(.*?)\*\*\*/g, '<strong><em>$1</em></strong>') .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>') .replace(/\*(.*?)\*/g, '<em>$1</em>'); // Tách theo dòng \n const lines = cleaned.split('\n'); return ( <div className="flex flex-col gap-2"> {lines.map((line, idx) => { const trimmed = line.trim(); if (!trimmed) return null; // Nếu là dạng danh sách (bullet point) if (/^[*-]\s+/.test(trimmed)) { return ( <div key={idx} className="flex items-start gap-2 ml-2"> <span className="text-[#1A1A1A] mt-0.5 text-[10px]">●</span> <span className="flex-1" dangerouslySetInnerHTML={{ __html: trimmed.replace(/^[*-]\s+/, '') }} /> </div> ); } return <p key={idx} dangerouslySetInnerHTML={{ __html: trimmed }} />; })} </div> ); }; export const TypingIndicator = () => { const container = useRef<HTMLDivElement>(null); useGSAP(() => { gsap.to(".typing-dot", { y: -4, duration: 0.4, stagger: 0.15, yoyo: true, repeat: -1, ease: "power1.inOut" }); }, { scope: container }); return ( <div ref={container} className="flex items-center gap-1.5 h-6 px-1 mt-1"> <div className="typing-dot w-1.5 h-1.5 bg-[#1A1A1A]"></div> <div className="typing-dot w-1.5 h-1.5 bg-[#1A1A1A]"></div> <div className="typing-dot w-1.5 h-1.5 bg-[#1A1A1A]"></div> </div> ); }; interface AIChatMessageProps { msg: ChatMessage; isChatting: boolean; isGenerating: boolean; onGenerateRedirect: (userContext: string) => void; fullChatContext: string; } export function AIChatMessage({ msg, isChatting, isGenerating, onGenerateRedirect, fullChatContext }: AIChatMessageProps) { const hasRedirectAction = msg.role === 'ai' && msg.content.includes('[ACTION:REDIRECT_OUTFIT]'); const cleanContent = msg.content.replace(/\[ACTION:REDIRECT_OUTFIT\]/g, ''); return ( <div className={cn("chat-intro flex flex-col gap-1.5 w-full", msg.role === 'user' ? "items-end" : "items-start")}> {msg.role === 'ai' && ( <div className="flex items-center gap-2"> <span className="text-[9px] font-bold text-[#A3A3A3]">CLOSY AI</span> <span className="text-[9px] text-[#A3A3A3]/70">{new Date(msg.timestamp).toLocaleTimeString('vi-VN', { hour: '2-digit', minute: '2-digit' })}</span> </div> )} {msg.role === 'user' && ( <div className="flex items-center gap-2"> <span className="text-[9px] text-[#A3A3A3]/70">{new Date(msg.timestamp).toLocaleTimeString('vi-VN', { hour: '2-digit', minute: '2-digit' })}</span> <span className="text-[9px] font-bold text-[#A3A3A3]">YOU</span> </div> )} {(!cleanContent && msg.role === 'ai' && !isChatting) ? null : ( <div className={cn( "text-[13px] leading-relaxed", msg.role === 'user' ? "p-4 bg-[#1A1A1A] text-white ml-6 md:ml-12" : (msg.role === 'ai' && !cleanContent && isChatting) ? "py-2 px-1 mr-6 md:mr-12" : "p-4 bg-[#F9F9F9] text-[#1A1A1A] border border-[#E5E5E5] mr-6 md:mr-12" )}> {msg.role === 'ai' && !cleanContent && isChatting ? ( <TypingIndicator /> ) : ( msg.role === 'ai' ? formatAIResponse(cleanContent) : cleanContent )} </div> )} {hasRedirectAction && ( <div className="mt-2 mr-6 md:mr-12"> <button onClick={() => onGenerateRedirect(fullChatContext)} disabled={isGenerating || isChatting} className="bg-[#1A1A1A] text-white text-[11px] font-bold py-3 px-6 hover:bg-black/80 transition-colors flex justify-center items-center gap-2 disabled:opacity-50 shadow-sm border border-[#1A1A1A]" > <Sparkles className="w-3.5 h-3.5" /> TẠO BỘ PHỐI ĐỒ NGAY </button> </div> )} </div> ); }
